\documentclass[landscape]{article}
\usepackage[a6paper, margin=0pt]{geometry}
\setlength{\parindent}{0pt}

\begin{document}
\pagenumbering{gobble}
{% for bingo in bingos %}
  \vspace*{5mm}
  \begin{center}
  \Huge{Sant Joan al Barri}
\end{center}

  \begin{tabular}[c]{|c|c|c|c|c|}
  \hline
  {% for line in bingo.lines %}
    {%+ for song in line.songs +%}
      \parbox[c][25.351mm][c]{25.351mm}{ \centering
        \large{  {{ song | replace('&', '\&') }} }
      }{%+ if not loop.last +%} & {% endif %}
    {% endfor %}
    \\
    \hline
  {% endfor %}
  \end{tabular}
  \newpage
{% endfor %}

\end{document}
